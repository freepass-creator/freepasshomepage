# Candidate evidence copies

Every binary and SVG in this directory is copied byte-for-byte from an observed source. Verify hashes against `../../brand-manifest.json`.

These are not approved masters. Do not redraw, recolor, merge, normalize, or publish them as a final CI family until the manifest status and the individual asset status are both `APPROVED`.
